---
name: codex-hud:configure
description: Apply codex-hud display preset (full, essential, minimal).
allowed-tools: Bash, Read
---

Configure codex-hud using preset:

```bash
npm run build
node dist/index.js configure --preset full
```

Available presets: `full`, `essential`, `minimal`.
